# DESIGN-check-6.md · 严格自检第 6 轮报告(原文完整留存)

> 执行者:「干净的眼睛」——无讨论上下文,仅依据磁盘文件。第 6 轮,不受前五轮锚定。
> 裁决:**FIX**(P2×3,无 P0/P1)
> 趋势对比:r1 13 → r2 9 → r3 6 → r4 7 → r5 3 → r6 3(首次无 P0/P1)。

---

# 独立核查报告(第 6 轮)

裁决:**FIX(P2×3,无 P0/P1)**
趋势:r1 13 → r2 9 → r3 6 → r4 7 → r5 3 → r6 3。三项均为 v1.5 新机制(tmp+原子改名)自身引入的闭合缺口,另有一项前五轮漏检问题。

## 一、v1.5 三项修复验证

1. check-5 P1(行 4 半份矛盾):撰写/修复均改 tmp+原子改名,行 4 状态列删"或只写了一半"、§7.3① 删"或半份",回到纯存在性判定 ✓——但新机制自身带来问题 1、2;
2. check-5 P2(AUTHORIZATION.md 格):§5.4 改"AI 写入一律拒绝,无任何批准路径",与 §6.1/§7.4/§10 四处对齐,完整 ✓;
3. check-5 P2(列名):§6.3 与 §6.4 统一为「批注id / 原文摘录 / 回应」✓。

## 二、问题清单

**1|P2|§5.4 矩阵 vs §7.3①②/§7.4 行4(tmp 机制的执行者与矩阵归属未定义)**
v1.5 规定撰写调用"写 `DESIGN.md.tmp` 后原子改名",但两步均无执行者指派、未映射进"完整矩阵":AI 写 `DESIGN.md.tmp`(项目根、docs/ 外)按矩阵字面落"项目内、docs/ 与 DESIGN.md 之外→弹窗"行,与同行 DESIGN.md 格"授权即用户明确许可、自动放行"的意图相抵——授权后落盘反而每次弹窗;改名若由 AI 以命令执行(如 mv)则触发"执行任何命令→弹窗"行。对照:同文档为 AUTHORIZATION.md 明确了"仅后端"执行者,tmp 却没有。
修法:钉死执行者并扩一行:AI 写 `DESIGN.md.tmp`(矩阵 DESIGN.md 格括注"含其 .tmp 暂存")、改名由后端在调用成功收尾时执行(同 AUTHORIZATION.md 先例)。

**2|P2|§7.3②("半份修订"兜底句与 tmp+原子改名自相矛盾)**
同一括注两说:前半保留"修复者已改到 DESIGN.md 中途被杀→半份修订被下一轮核查看见"的兜底(该状态只在直接写或多段落盘下存在),后半(v1.5 新增)规定修订"同样走 tmp+原子改名"——单次 tmp+改名下"半份修订"不可能存在,"尽量缩小该窗口"无指称对象;且全文未规定修复者是单次落盘还是多次落盘,两种读法导出不同实现。同一机制在 §7.4 行4 得"半份不可能存在"的绝对保证、此处却只得"缩小窗口"的弱保证。低风险(DESIGN.md 恒存在于阶段 5,推导表不受影响),但迫使读者自行发明缘由。
修法:钉死写盘纪律:按"单次落盘"则删"半份修订……循环自身兜底"句;按"多次落盘"则改写为"中断时已落盘的修订完整有效,未落盘的修复项重现于下一轮报告"。

**3|P2|§5.5/§7.3 第一条 vs §7.4 行3、§4.4 G2(轮次生产中断的"重跑同轮覆盖"无实现路径,前五轮漏检)**
批量处理被杀留下半份 `discuss-round-(N+1)` 时:①§7.4"当前轮=最大 N"无完整性判据地把它认作当前完整轮(§6.4 授权标记末行本可判完整,但未接入推导);②round-N 批注仍是 pending(后端仅在新轮文档完整产出后才回写,§6.2),而本轮(N+1)自身无批注,「处理本轮批注」(阶段 3 唯一驱动,§3.3/§4.4)无可处理对象;③重跑产物命名无论 N+1(覆盖,无规则支撑其识别半份)还是 N+2(半份 N+1 永久冻结留档)都无据可依——后者直接违背"重跑同轮直接覆盖、自愈"(D-13)的承诺,且半份轮次违背成功判据 3"每一轮落盘可查"的存档质量。前五轮核查(含 check-3 问题 6 的"阶段 5 重跑入口")只补了阶段 5 各调用,轮次生产这一路径从未过检。
修法:补一句:最新轮次文档若无合规末行标记(§6.4)即判半成品,视为不存在——当前轮取最大完整轮、其 pending 批注照常由「处理本轮批注」处理、产物覆盖原编号落盘。

## 三、五维结论

1. **内部一致性:有问题。** 问题 1(矩阵与 tmp 两步的归属矛盾)、问题 2(同段两种保证强度)为文本级缺口;主口径(G3 四处校验单一权威、推导表互斥+首条命中+行4 回归纯存在性、核查/修复双角色、PASS 前缀文法、完成态、权限格四处对齐、列名统一)逐项对照自洽。
2. **历史批注符合度:零问题。** v1.5 三项改动逐一对 D-01~D-21:权限格收紧与列名统一为定点修补;tmp 机制不与任何已确认决策冲突(D-13 确认的是轮次文档"覆盖自愈",DESIGN.md 写入路径从未有用户裁决,该项属空白地带的修复期补全,方向服务于 D-19"文件即状态"),无越出边界的发明;D-20/D-21 溯源与追溯表记载一致。
3. **决策无歧义:有问题。** 问题 1(tmp 谁执行、弹不弹窗)、2(修复者写盘纪律)、3(半份轮次的当前轮推导与重跑命名)会迫使陌生工程师追问并自行发明;其余决策均可独立读懂。
4. **可实施性:主体可开工。** 三项均为一句级补写,无架构阻断;问题 1 若按矩阵字面实现,授权后的撰写/修复落盘将逐次弹窗,与该行自身设计意图相悖,应在开工前修掉。
5. **结论覆盖:零问题。** 追溯表 21 行逐一核对,溯源与"本文位置"全部真实指向;21 条已确认决策正文均有承载;v1.5 对上轮 3 项修复全部完整(问题 1、2 系新机制自身引入的缺口,问题 3 为历史漏检)。

> 核查结论:FIX(P2×3)
