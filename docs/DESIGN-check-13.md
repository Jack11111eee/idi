# DESIGN-check-13.md · 严格自检第 13 轮报告(原文完整留存)

> 执行者:「干净的眼睛」——无讨论上下文,仅依据磁盘文件。第 13 轮,不受前十二轮锚定。
> 裁决:**FIX**(P2×3,无 P0/P1)
> 趋势对比:13→9→6→7→3→3→3→2→2→3→3→2→3。
> **本轮为循环末轮:**用户于 r13 启动后裁决变更收敛标准为「残余裁决制」(D-22)——纯 P2 轮不再自动循环,残余问题逐条交用户裁决「修」或「接受现状」。本报告三项残余问题即按 D-22 交裁决,不再启动第 14 轮自动核查-修复循环。

---

# DESIGN.md v1.12 · 第 13 轮独立核查报告

**总裁决:FIX(P2×3,无 P0/P1)**

核查对象:DESIGN.md v1.12(对照 discuss-round-0~4、DESIGN-check-1~12)。趋势:13→9→6→7→3→3→3→2→2→3→3→2→r13·3。

## 一、v1.12 两项修复验证

1. **裁决待续跑态判定式(对 check-12 #1):** 落实 ✓。§6.4 判定式②定义成立要件与退出(更新编号报告出现),§7.3②/§8.2"裁决落盘期"均换称并指回;宽松档含裁决轮由②正确承载(无更新报告、修复未续跑→「继续修复」)。但①②并立引出问题 1,与暂停态呈现规格的旧句相撞引出问题 2。
2. **扫描范围与排他(对 check-12 #2):** 落实 ✓。"结论行之后"+完整匹配 `#数字:` 形态双重排他——现存 12 份报告头部 `> 裁决:**FIX**…` 行(位于结论行之前、无 #K 形态)双重排除,假配对通道封死;模板禁令+行内代码引用为第二道保险。但"结论行"锚点在多结论行报告(宽松档)下取哪一处未钉死,引出问题 3。

两项均在既有裁决落盘通道方向内,未触动任何用户可见决策。

## 二、残余问题清单(按 D-22 交用户裁决)

**1|P2|§6.4 判定式①②:同轮多组问答场景两式同真,无互斥条件或优先声明**
- 位置:§6.4"判定式:①存在未配对的 `待裁决:#K:` 行→暂停态……②最新报告存在配对问答行、且不存在更新编号的报告→裁决待续跑态"
- 描述:文法显式支持同轮多问(K 递增)。可达序列:#1 抛出→裁决落盘(配对)→「继续修复」重跑→修复者再抛 #2(未配对)——此时①②**同时成立**:①要求呈现问题与输入框,②要求呈现「继续修复」,两态交互规格互斥。文档先例(§5.4、§7.4 两表头均显式声明"自上而下首条命中";check-4 #3、check-8 #1 均按同形问题计过)表明静默优先不被认为足够。按②优先实现的界面不出现输入框,未答的 #2 无作答通道,用户被困在"「继续修复」→重抛→幂等不再落盘"的循环里。
- 建议修法:②补一个互斥条件"且无未配对的待裁决行";或在判定式前声明首条命中(与 §5.4/§7.4 同款表头声明)。

**2|P2|§8.2 与 §6.4①:暂停态(期)的界面控件清单两节互斥,v1.12 新增①未同步 §8.2**
- 位置:§8.2"暂停期与裁决已落盘期间……隐藏「继续自检」、**只呈现「继续修复」**" vs §6.4①"→ 暂停态(隐藏「继续自检」、**呈现问题与输入框**)"
- 描述:同一状态两套规格,命名元素零重叠:§8.2 以"只"排除其它控件、只字未提输入框;①以并列构造枚举暂停态呈现、未含「继续修复」。两读皆可实施:按 §8.2 字面则暂停期无输入框(用户无法作答,裁决通道死锁——§8.2 同段"用户在界面回答后"落空);按 ①则暂停期无「继续修复」(反而避开 check-10 P2-2 已记的"裁决前误点重跑"分支)。两个实现者做出不同界面,其一死锁。v1.12 在 §6.4 新写①时未回改 §8.2 的旧句。
- 建议修法:两处统一为"暂停态 = 隐藏「继续自检」、呈现问题与输入框(裁决落盘前不呈现「继续修复」)";§8.2 的"只呈现「继续修复」"改归裁决待续跑态一态。

**3|P2|§6.4"只统计结论行之后":多结论行报告(宽松档)中"结论行"锚点二义,完成态与②正面冲突**
- 位置:§6.4 扫描范围条款 + PASS 文法;对照 §7.4 末行"使命完成"、§8.2 宽松档收尾。
- 描述:宽松档含裁决轮(check-9 已认可场景)的最终报告为:`> 核查结论:FIX(…)`、`> 待裁决:#1:…`、`> 裁决:#1:…`、`> 核查结论:PASS(…)`(修复者追加,check-10 已认可双结论行并存)。"结论行"取哪一处未定义:按首处 `> 核查结论:` 行计,配对空间含已答问答对→②成立(宽松档永无更新编号报告),而末行为 PASS→§7.4 使命完成(只读归档、无按钮)——同一磁盘现状被导出"只读归档"与"呈现「继续修复」"两个互斥状态;按末处计则无冲突。锚点还有第三读(PASS 文法定义"结论行 = 报告最后一个非空行"——据其实现则追加后判定式全体失效),文档仅以"这些行不是结论行"排除了该读,未排除首/末之选。
- 建议修法:②补条件"且最新报告末行非以 `> 核查结论:PASS` 开头";同时把"结论行"钉死为"最后一处 `> 核查结论:` 行"。

## 三、五维结论

1. **内部一致性:有问题(1、2、3)。** v1.12 两项修复自身落地正确,头部裁决 `> 裁决:**FIX**` 行的假配对风险已双保险封死;主口径(G3 四处校验单一权威、推导表互斥+首条命中、矩阵首条命中、tmp/直写/AUTHORIZATION 四处对齐、PASS 前缀文法、两跳触发、完成态)逐项对照自洽;残留矛盾集中在判定式①②之间、①与 §8.2 之间、"结论行"锚点三处。
2. **历史批注符合度:零问题。** D-01~D-21 逐条与讨论原文及前十二轮裁决记录对照,收录无走样;v1.12 两机制是对 v1.8~v1.11 既有裁决落盘通道的判定式补全,服务于"获用户裁决后才继续"与 §7.2 文件即状态,未越出任何已确认决策边界;D-20/D-21 溯源与追溯表记载一致。
3. **决策无歧义:有问题(1、2、3)。** 三处均迫使实现者自行选择:①②优先序、暂停态控件清单、"结论行"锚点;分别选错导致未答问题无作答通道、裁决通道死锁、完成态被路由为可操作态。
4. **可实施性:主体可开工。** 三项均为一句级钉死补写,无架构阻断;问题 2 若按 §8.2 字面实现即裁决无法作出、问题 3 使已完成项目永不进入归档态,应随下轮修掉。
5. **结论覆盖:零问题。** 追溯表 21 行逐一核对,溯源与"本文位置"全部真实指向;头部宣称修复计数 13/9/6/7/3/3/3/2/2/3/3/2 与十二份报告裁决行逐一吻合;v1.12 两项修复与 check-12 问题清单一一对应。

另查未计,沿先例放行:②在"修复已完成、下一轮核查在途"的重叠窗口内仍成立,点击「继续修复」会重跑已完成的修复——check-12 问题描述中已明示并权衡该代价(行为安全、幂等兜底),判既定取舍不重计;"报告头部的结论行"措辞别扭(意为头部诸行、结论行及正文均不进配对空间),操作规则不受影响,判措辞级;暂停态/裁决待续跑态/「继续修复」未入 §10 术语表,无行为分歧,判完善项。

> 核查结论:FIX(P2×3)
