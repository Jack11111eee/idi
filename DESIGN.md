# DESIGN.md · 交互式讨论迭代系统 · 总设计文档

> 属性:**唯一权威文档**。本文是本项目讨论流程(discuss-round-0 ~ 4)的最终收敛产物。
> 版本:v1.6。初稿内容来自 21 条已确认决策(D-01~D-21);v1.1~v1.6 分别按第 1~6 轮核查报告修复 13/9/6/7/3/3 项(各报告见 docs/DESIGN-check-1~6.md),其中 v1.3 撤销了 v1.2 越出 D-17 边界的"授权后免确认词"规则。修复均为定点补写,不改变已确认决策的方向。
> 可选后续:基于本文追加分模块文档做细节拆分,模块划分与内容自行判断,但**不得与本文冲突**。
> 溯源:各决策的讨论过程见 `docs/discuss-round-N.md`。

---

## 1. 项目概览

### 1.1 要解决的问题

用 AI 做项目,最大的浪费不是写错代码,而是**开工前没对齐**:AI 自作主张选了方向,或双方各自以为理解了对方。本工具把"动手前对齐"变成固定流程:想法进、无歧义的总设计文档出。

### 1.2 目标一句话

把「构建前的讨论、细化、对齐」做成一个**有可视化界面、可批注、可追溯、以总设计文档为终点**的本地工具。

### 1.3 成功判据

1. 产出的总设计文档不存在未决问题,且通过所选档位的自检;
2. 用户全程不因 AI 语言晦涩而卡住(划词即可要大白话);
3. 每一轮讨论、每一条批注都落盘可查;
4. 未经用户明确授权,流程绝不进入"撰写总设计文档"步骤。

### 1.4 流程终点

总设计文档通过自检、界面提示「使命完成」,流程正式结束,此后进入只读归档态(§7.4)。**代码实施不在本工具职责范围内。**

### 1.5 范围内 / 范围外

**内:** 五阶段讨论流程、划词批注界面、发散机制、文档与批注落盘体系、自检机制、授权门。

**外(非目标):** 写代码实施、多项目并行、多人协作、云端部署、批注以外的文档编辑能力、AI 生成中途插话(v2 再议,D-20)。

## 2. 用户与场景

- **用户画像:** 单机、单人、本地运行 (D-03);讨论文档以中文为主。
- **两种打开方式 (D-15):**
  - **零起步:** 项目尚未开始,从想法出发走完整五阶段;
  - **中期接手:** 项目进行到任何时候打开同一工具、走同一流程,阶段 1-2 的讨论基于**现有代码现状**展开(AI 可读项目文件,权限门 §5.4 照常生效)。
  - 中期接手**不做任何专门机制**,流程天然支持,仅在使用说明中写清该场景。

## 3. 核心流程(五阶段、三道门)

### 3.1 阶段总表

| # | 阶段 | 输入 | 产出 | 通过条件(门) |
|---|------|------|------|----------------|
| 1 | 需求与目标 | 一句话想法(或空白) | 需求/目标陈述 | 无独立门——阶段 1-2 为一段连续会话,唯一正式出口是 G1 |
| 2 | 雏形 | 目标陈述 | 雏形草稿(`docs/draft.md`) | **G1**:用户点「认可雏形」(交互见 §4.4) |
| 2.5 | 发散模式(可选) | 没想法 | 候选方向 → 雏形 | 同 G1 |
| 3 | N 轮迭代收敛 | 雏形 | 每轮一份 `discuss-round-N.md` | **G2**(每轮):该轮批注全部被回应;整个阶段的出口 = 未决清单清零 + 维度表全绿(§3.6)→ 申请授权 |
| 4 | 撰写总设计文档 | G3 授权 | `DESIGN.md` | **G3**:用户**明确授权**(默认拒绝) |
| 5 | 自检(宽松/严格) | `DESIGN.md` | 修订版 + 核查报告 | 宽松档:核查+修复+报告即完成;严格档:连续一轮零问题(§8.2) |

### 3.2 单一界面 (D-08)

全程同一界面(见 §4),不设独立页面承载任何阶段,发散模式亦不单独成页。

### 3.3 N 轮收敛的驱动:未决清单

AI 维护一份**未决问题清单(ledger)**,每轮固定四步:

1. 逐条回应上轮全部实质批注(大白话请求在产生时已即时回答,不进入轮次循环);
2. 更新文档(本轮版次),已定决策标注"已对齐";
3. 追问新的更深层细节问题,进入清单;
4. 文末公开当前未决清单。

**收敛判据(与 §4.4 / §8.1 单一口径):** 当前轮满足「上轮批注已全部被回应 + 未决清单清零 + 覆盖维度表全绿」→ AI 在本轮文档末尾写入授权申请标记,请求授权(G3);未满足则开启下一轮。轮次由用户点「处理本轮批注」驱动,每轮通常携带新批注——**新批注的存在不阻碍申请授权**(回应完毕即为"已全部被回应"),不存在"必须静默一轮"的要求。

### 3.4 双轨批注 (D-05)

- **大白话提问 → 即时回答。** 划选 → 提问 → 数秒内出解释,不打断阅读。**调用形态:** 轻量无头调用,仅携带当前打开的文档与划选原文(不读全量 docs/、不走逐轮四步),保证秒级响应;
- **实质批注 → 批量处理。** 用户批注完所有想批的,点「处理本轮批注」,AI 一次性看到全部批注统一回应,并产出新一轮文档。

### 3.5 轮次冻结 (D-07)

一轮文档在该轮结束(产出下一轮快照)后即冻结,成为只读归档;**批注永远挂在它诞生的那轮文档上**,永不迁移;新一轮文档由 AI 在回应中显式引用"上轮批注#N"接续讨论。被批注的文本永不变化,批注不存在"漂移"。

### 3.6 收敛判据客观化:覆盖维度表 (D-09)

固定维度清单:`目标与边界 / 用户与场景 / 流程与状态 / 数据与落盘 / 异常与失败处理 / 非目标 / 术语表`。每个维度必须有对应文档小节、且无未决问题,全部打勾后 AI 才**有资格**请求授权(G3)。维度表本身可在讨论中增删。授权按钮点亮条件以 **§4.4 为权威定义**(四处机械校验,本节维度表为其一;文法见 §6.4)。
**诚实的边界:** 各处标记仍由 AI 撰写——本机制把"AI 自评"约束为固定文法、由工具机械校验,并非根除自报;最终否决权始终在用户(授权须输入确认词)。

### 3.7 发散模式 (D-16)

- **入口:** 仅限阶段 1-2(雏形诞生前)。新建项目时选「没想法」进入;一旦雏形存在,发散模式关闭——后续分歧一律走批注;
- **实现:** 内置「发散指令模板」(专用提示词),跑在 §5 的同一调用链路上,过程照常直播到工作面板。**模板要点**(源自 round-0 §6,随 D-14/D-16 定案):①多视角风暴——从 N 个固定视角(如:解决谁的什么痛点 / 最省事的版本 / 最贵的版本 / 没人做但该有人做的)各出 2~3 个方向,鼓励离谱;②收敛——汇成 3~5 个候选方向,每个含「一句话说明 + 一句为什么值得做」;③引导用户挑选或委托 AI 挑选;
- **产物:** `docs/brainstorm.md`。草稿文件,可反复覆盖,不编号、不冻结、不进未决清单;
- **出口:** 用户挑一个候选方向(或委托 AI 挑),深化为雏形,回到主干。

### 3.8 语言红线(全阶段 prompt 约束)

> 输出必须简洁、精准,不用晦涩词语。但解释必须清晰、完整——简洁不等于省略,不许因追求简短而含糊带过。未定义的术语不用;必须用术语时,先用一句大白话定义。一次讲一个小点,讲透。

此约束注入流程的**每个阶段**(讨论、发散、写文档、自检)。

## 4. 界面设计

### 4.1 布局

```
┌─ 文档区(左/主) ────────┬─ 侧栏(右) ────────────┐
│  ...正文... 划词→弹菜单   │ ◉ 批注流               │
│  (高亮 = 有批注)        │   #3 已回应 · #4 待处理 │
│                        │ ◉ AI 工作面板(直播)     │
│                        │   ▸ 读取 round-1   ✓   │
│                        │   ▸ 回应批注#3 ...      │
│                        │   ▸ 写 round-2 ...      │
│                        │ [中止]   [处理本轮批注] │
└────────────────────────┴────────────────────────┘
```

(D-08, D-10, D-11;划词批注为必开发功能 D-02,排期独立于为本项目自举服务)

### 4.2 划词批注交互

- 划词 → 弹小菜单 →「批注」或「用大白话讲这段」;
- 批注与被选原文绑定存档,AI 回复附在旁边;已解决批注变灰但不删除;
- 每轮侧栏显式"本轮批注未处理数";全部被回应后,本轮随下一轮文档的产生自动冻结(G2,见 §4.4)——无单独"标记结束"操作;
- 阶段 1-2 时,文档区显示会话产出的草稿(`draft.md`)、侧栏为对话流(此时无划词批注;对话全程逐条转录落盘为 `docs/transcript.md`,是阶段 1-2 会话的恢复依据,见 §6.1);进入轮次后切换为轮次文档 + 批注流。

### 4.3 AI 工作面板 (D-11)

展示**全部原始事件**(每条工具调用与结果),可折叠收起。透明优先,兼作调试出口。

### 4.4 三道门的交互

- **G1 认可雏形:** 阶段 1-2 的草稿区末尾常驻「认可雏形」按钮。点击即 G1 通过:当前草稿定稿为 `discuss-round-1.md`(雏形即第 1 轮文档,可继续被批注),进入阶段 3。此前的修改意见通过会话流给出;
- **G2 轮次结束:** 无独立按钮——用户点「处理本轮批注」后,AI 批量回应全部批注、产出 `discuss-round-(N+1).md`;本轮批注全部转为已回应、下一轮文档产生,**本轮即自动冻结**(即 D-07 的冻结时机);
- **G3 授权 (D-17):** 「授权撰写总设计文档」按钮点亮条件(本文**权威定义**,§3.6/§8.1 均指回此处)= 工具机械校验以下四处全部通过:①当前轮 annotations.json 中无 `status: pending` 的实质批注;②轮次文档未决清单清零;③维度表全绿;④授权申请标记为「是」(②③④文法见 §6.4)。点击 → 确认框 → 输入确认词「确认授权」二次确认,防误触;默认拒绝。拒绝 = 一条普通批注(写明还差什么),继续下一轮。

## 5. AI 集成层

### 5.1 无状态、文件驱动 (D-10)

每次任务 = 一次**全新无头调用**。AI 启动时自磁盘读取 `docs/` 下全部文档与批注(阶段 1-2 时含 `transcript.md` 会话转录、`draft.md` 草稿),产出写回磁盘——**所有记忆都在文档里**。中途任何失败,重跑同一条任务即可,可复现。(大白话问答是例外:走 §3.4 的轻量调用。)

### 5.2 事件直播 (D-10)

无头调用的事件流(AI 说的每句话、每次读文件、每次写文件)由后端原样转发到浏览器,渲染进工作面板。从用户点「处理本轮批注」起全程可见,不是黑箱等结果。

### 5.3 实现路线 (D-01, D-10)

- **首选:** Python 端用 Claude Agent SDK(进程内结构化事件流 + 权限回调);
- **兜底:** 子进程 `claude -p --output-format stream-json`;
- 两条路线**界面契约完全一致**,可替换。使用前提:本机已装 claude CLI 并登录。

### 5.4 权限门 (D-10,完整矩阵)

| 操作 | 对象 | 处置 |
|------|------|------|
| 读 | 项目内任意文件 | 自动放行 |
| 读 | 项目外任何路径 | 弹窗 |
| 写 | `docs/` 内 | 自动放行 |
| 写 | `DESIGN.md` 及其 `.tmp` 暂存(项目根) | 自动放行(G3 授权本身就是用户对写这个文件的明确许可,阶段 5 自检修订由同一许可覆盖;.tmp 为撰写/修复调用的暂存形态;**原子改名为 `DESIGN.md` 由后端**在调用成功收尾时执行,AI 不执行改名) |
| 写 | `AUTHORIZATION.md`(项目根) | **仅后端**在确认词交互通过瞬间写入;**AI 写入一律拒绝,无任何批准路径**(它是授权唯一凭证,见 §7.4——不能容 AI 以任何形式绕过 D-17 防误触) |
| 写 | 项目内、docs/ 与 DESIGN.md 之外 | 弹窗 |
| 写 | 项目外任何路径 | 弹窗 |
| 执行 | 任何命令(含项目内,如 git) | 弹窗 |

中期接手(D-15)"流程天然支持"依赖第一行:AI 读项目代码自动放行,不因权限门打断。

### 5.5 中止 (D-10, D-13)

「中止」按钮随时杀掉当前 AI 调用。文件驱动设计保证杀掉无损;中断残留的半成品由重跑同轮覆盖,自愈(§7.3)。**不做**生成中途插话(双向流),v2 再议(D-20,已确认为非目标)。

## 6. 数据设计

### 6.1 目录结构 (D-04)

```
<项目目录>/
  DESIGN.md                     # 总设计文档(唯一权威,项目根)
  AUTHORIZATION.md              # G3 授权痕迹(后端在确认词通过时写入,非 AI;授权唯一凭证,见 §7.4)
  docs/
    transcript.md               # (仅阶段 1-2)会话逐条转录:双方发言追加式落盘,重启即由此恢复
    draft.md                    # 阶段 1-2 会话产出的草稿(可覆盖;G1 后定稿为 discuss-round-1.md)
    discuss-round-N.md          # 各轮讨论文档(轮末冻结)
    discuss-round-N.annotations.json
    brainstorm.md               # 发散草稿(可覆盖)
    DESIGN-check-N.md           # 自检报告(宽松 1 份;严格每轮 1 份)
```

**transcript.md 格式(机器可解析,§6.4 同族文法):** 每条消息 = 起始行 `[user]` 或 `[ai]`(独占一行),其后为消息体(可为任意多行/多段落),以下一条起始行或文件末尾为界——多行消息体不需转义。后端在用户发送/AI 事件回落时追加,并**不**在 G1 后删除(保留为阶段 1-2 的完整历史,满足成功判据 3「每一条都落盘可查」)。

### 6.2 批注数据结构 (D-18)

`docs/discuss-round-N.annotations.json`:

```json
{
  "round": 3,
  "items": [
    {
      "id": "a3-01",
      "quote": "被划选的原文(精确文本)",
      "before": "原文前 40 字(选中文本重复出现时辅助定位)",
      "type": "comment | plain",
      "note": "用户写的话",
      "status": "pending | answered",
      "answer": "AI 的回应(plain 即时答;comment 批量回应)",
      "created_at": "ISO-8601"
    }
  ]
}
```

- `type: plain` = 大白话问答;`comment` = 实质批注,进未决清单驱动收敛;
- 定位靠精确文本匹配(`quote` + `before` 辅助);轮次冻结(D-07)保证被批注文本永不变化,无需更复杂算法;
- 大白话问答**落盘**且样式区分(灰色斜体),**不计入未决清单** (D-12);
- **字段写回职责:** 批注条目由**后端**经界面创建(quote/before/type/note,status=pending);批量处理完成后,后端解析 AI 产出的新一轮文档中的批注回应表,统一回写 `answer`、把 `status` 置为 `answered`——AI 子进程不直接改写 annotations 文件,避免格式漂移。大白话即时答同样由后端落盘(type=plain 的 `answer` 即时写回),不走 AI 写文件路径。

### 6.3 轮次文档约定

每轮 `discuss-round-N.md` 固定包含五件套:批注回应表、决策登记(实践归纳:沿用本项目讨论流程的自然做法,非独立决策的产物)、**维度表**、**未决清单**、文末**授权申请标记**。后三者的机器可解析文法见 §6.4,支撑 §4.4 的按钮逻辑。
**批注回应表格式(机器可解析,§6.4 同族文法;与决策登记同属实践归纳):** 固定列 = 批注id / 原文摘录 / 回应;批注id 与**上一轮** `discuss-round-(N-1).annotations.json` 中的 `id` 一一对应(注:此 N 指本回应表所在文档的轮次,即回应表回应的是 N-1 轮的批注),后端据此回写(见 §6.2)。

### 6.4 机器可解析文法(工具与 AI 共同遵守)

工具按下述固定文法解析轮次文档的四个结构;AI 生成轮次文档的模板必须逐字遵守。**适用范围:** 本文法约束本工具实施后新生成的一切轮次文档;实施前的历史文档(如本项目自身的 discuss-round-1~4)不要求回溯改造,工具对它们不做按钮判定(本项目在工具建成前用人工对话完成流程,不是验收样例)。

| 结构 | 位置与标题 | 行格式 | 判定 |
|------|-----------|--------|------|
| 维度表 | 二级标题,标题包含「覆盖维度表」 | 表格,列 = 维度 / 状态 / 说明;状态 ∈ {✓, ◐, ✗} | 全绿 ⇔ 状态列无 ◐ 与 ✗ |
| 未决清单 | 二级标题,标题包含「未决问题清单」 | 表格,列 = 编号 / 问题 / 状态;状态 ∈ {待决, 已决} | 清零 ⇔ 无状态为「待决」的行 |
| 授权申请标记 | 文档最后一个非空行 | `> 申请授权:是` 或 `> 申请授权:否` | 申请 ⇔ 恰为「> 申请授权:是」 |
| 批注回应表 | 二级标题,标题包含「批注回应」 | 表格,列 = 批注id / 原文摘录 / 回应;id 对应上一轮 annotations.json | 后端据此回写 answer/status(§6.2) |

**PASS 结论行文法(自检报告,工具判定完成用):** 报告最后一个非空行**以 `> 核查结论:PASS` 开头**(前缀匹配;其后可附括注,如 `> 核查结论:PASS(问题 N 项修复完毕)`——写作模板须给出正例 `> 核查结论:PASS …` 与反例 `FIX(…)`/`BLOCK(…)`);核查执行者仅在零问题轮写 PASS,其余轮末行为 FIX 或 BLOCK 及计数。

## 7. 生命周期与恢复

### 7.1 启动

- 应用启动时自检 claude CLI(已装 + 已登录),不满足给出指引,只挡第一次;
- 启动流程:打开应用 → 选择或输入项目目录 → 进入单界面;
- 所选目录的流程状态由磁盘现状推导,规则见 §7.4 (D-19)。

### 7.2 恢复哲学:文件即状态 (D-19)

所有状态持久化在磁盘文件上。重启、崩溃、关机,回来打开,状态完整(推导规则见 §7.4)。

### 7.3 崩溃与中断 (D-13, D-19)

- 服务器崩溃/被杀 → AI 子进程随之终止;半成品文件重跑同轮覆盖,自愈。**完整轮判据:**最新轮次文档若末行不是合规的 `> 申请授权:是|否` 标记(§6.4)即判为半成品,**视为不存在**——当前轮取最大完整轮,该轮的 pending 批注照常由「处理本轮批注」处理,重跑产物**覆盖原编号**落盘(D-13 承诺的实现路径);
- 批处理进行中关掉浏览器 → 服务端照常跑完并落盘;**直播错过就错过,不做回放**(不为一次性视流建档案系统);
- **阶段 5 中断的重跑入口:** ①撰写调用中断(有 `AUTHORIZATION.md`、无 `DESIGN.md`)→ §7.4 对应行提供「继续撰写」按钮,重跑重新写 `DESIGN.md.tmp`、由后端原子落盘,中断残留的 tmp 文件随重跑覆盖,自愈成立;②核查/修复调用中断 → 自检进行中状态下界面常驻「继续自检」按钮,点击重跑当前核查轮(半份 DESIGN-check-N.md 重跑覆盖;修复者对 DESIGN.md 的修订同样走"单次整体落盘"纪律——tmp 一次写全、后端改名——超时或失败时已落盘的 DESIGN.md 保持旧版完整有效,未完成的修复项会在下一轮核查报告中重现,循环自身兜底)。

### 7.4 阶段推导与完成判定 (D-19, D-21)

「文件即状态」的另一半:工具不维护独立的流程状态,一切由磁盘现状推导(下表**自上而下,首条命中即取**,行间条件已互斥化):

| 磁盘现状 | 推导状态 |
|----------|----------|
| 目录内无 `docs/` | 新讨论(阶段 1) |
| 有 `docs/`,无 `discuss-round-*.md`、无 `DESIGN.md` | 阶段 1-2 进行中(`draft.md` = 会话草稿,`brainstorm.md` 存在 = 发散草稿在场) |
| 有 `discuss-round-1..N`,无 `DESIGN.md`(N 取最大**完整**轮——末行含合规授权标记;末行不合格的最新轮判半成品、视为不存在,见 §7.3 完整轮判据),**且无 `AUTHORIZATION.md`** | 阶段 3,当前轮 = 最大完整 N(末轮授权标记只是 AI 的申请,授权凭证见下行的 AUTHORIZATION.md) |
| 有 `AUTHORIZATION.md`,无 `DESIGN-check-*.md`,无 `DESIGN.md` | 阶段 4(已授权,撰写未启动)。**半份 DESIGN.md 不可能存在:** 撰写调用以 `DESIGN.md.tmp` 写入、完成后原子改名为 `DESIGN.md`(D-13 同理推广),中断只残留 tmp,重开按本行显示「继续撰写总设计文档」,点击即重跑,残留 tmp 随之覆盖,无需重新确认词 |
| 有 `DESIGN.md`,无 `DESIGN-check-*.md` | 阶段 5 待选档:界面弹出宽松/严格选择(此时重开 → 重新弹选,幂等) |
| 有 `DESIGN.md` 与若干 `DESIGN-check-*.md`,最新一份末行非 PASS | 自检进行中(档位见最新报告头部),当前核查轮 = check 最大编号(宽松档半成品——报告已落盘未追加 PASS——同样落此行;半成品报告重跑覆盖) |
| 最新 `DESIGN-check-*.md` 末行以 `> 核查结论:PASS` 开头 | **使命完成** |

**授权痕迹文件(v1.3 新增,撤销 v1.2 越界规则):** G3 确认词交互通过时,**后端**(非 AI)在项目根写入 `AUTHORIZATION.md`(内容:授权时间 + 操作者确认词标记,明文,由后端生成;它的写入权限见 §5.4——AI 不可写)。它存在 = 用户确已授权;不存在 = 未授权——「末轮标记为是 + 无 DESIGN.md」重开时按**阶段 3 正常 G3 交互**处理(按钮重新点亮,重新走确认词),AI 写的申请标记永远不能替代用户授权凭证。

**完成标记的写入规则:** 严格档由核查执行者在其报告末行写 `> 核查结论:PASS`(仅当该轮零问题;有问题轮的报告保留原始问题清单);宽松档在唯一一轮核查+修复完成后追加 PASS 结论行(宽松档的定义即"核查+修复+报告即止",不要求零问题轮)。

**留痕说明:** 阶段 3→4 的推进以 `AUTHORIZATION.md`(后端在确认词通过时写入,见 §7.4 授权痕迹)为准——`DESIGN.md` 的出现 + 该痕迹共同构成授权链;申请的时刻由最后一份轮次文档的授权标记承载。

**完成态(D-21,只读归档):** 使命完成后再次打开项目,界面呈现**只读归档态**——可浏览全部轮次、批注、`DESIGN.md` 与核查报告;划词批注、「处理本轮批注」、授权按钮均不可用;不再追加轮次。要重新讨论 = 另起新目录从头走流程。

## 8. 授权与自检

### 8.1 授权(G3)

- **资格门槛:** 见 §4.4 权威定义(四处机械校验:annotations 无 pending + 清单清零 + 维度表全绿 + 标记为「是」——机械校验而非采信 AI 自评);
- **通过形式:** 点亮按钮 → 确认框 → 输入「确认授权」;默认拒绝;
- **拒绝形式:** 普通批注,写明还差什么,继续追加轮。

### 8.2 自检

- **档位选择时机:** DESIGN.md 初稿写完后,界面即弹出宽松/严格选择;选择结果记录于每份核查报告头部(落盘),重开据此恢复档位(尚未产生首份报告时重开 → 重新弹选,幂等);
- **档位:**
  - **宽松**——做一次全面核查,修复发现的问题,在报告中追加 PASS 结论行,即止;
  - **严格**——循环核查,直至**某一轮零问题、由核查执行者给出 PASS**才停,每轮报告留存;
- **多方角色(消除歧义):** 每轮循环 = 两个独立调用、一个角色分开的链:①**核查执行者**("干净的眼睛")——全新无头调用,以磁盘文件为唯一输入,产出该轮核查报告,并在零问题时于报告末行写 PASS;它**不修改 DESIGN.md**;②**修复者**——另一个全新无头调用(同样以磁盘为唯一输入,含刚落盘的核查报告),按报告执行修复、修订 DESIGN.md;修复期间若报告问题属于用户职权(如本工具的范围与非目标类问题),修复者必须停下来在界面抛出问题、获用户裁决后才继续(裁决照 D-20/D-21 先例补录为正式决策);宽松档仅在唯一一轮核查+修复完成后,由修复者在报告末追加 PASS 结论行;
- **核查五维:** 内部一致性 / 历史批注符合度 / 决策无歧义 / 可实施性 / 结论覆盖(全部决策无一遗漏);
- **产出:** `docs/DESIGN-check-N.md`(宽松 1 份、严格每轮 1 份)+ 修订后的 `DESIGN.md`;
- **终点:** PASS 结论行出现(判定规则见 §7.4)→ 界面弹「使命完成」提示,流程正式结束,此后为只读归档态(D-21)。

## 9. 技术栈与前提

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | Python + FastAPI (D-06) | 流式输出/网页推送一等支持,讨论回复边生成边显示 |
| 前端 | 原生 HTML/JS + markdown 渲染库 (D-06) | 单一主界面(D-08)+ 启动时的项目目录选择对话框;划词高亮用浏览器原生选区接口,引框架收益低 |
| AI 调用 | Claude Agent SDK 首选;`claude -p --output-format stream-json` 兜底 (D-10) | 界面契约一致,可替换 |
| 前提 | 本机已装 claude CLI 并登录 (D-19) | 启动时自检 |

## 10. 术语表

| 术语 | 大白话 |
|------|--------|
| 轮次(Round) | 一份文档快照 + 你的批注 + AI 回应的完整循环 |
| 雏形 | 阶段 2 产出的粗略项目样子,后续轮次负责磨细 |
| 批注 | 划选一段文字后写的话:提问、质疑或下指令 |
| 大白话问答 | 批注的一种:只求听懂,不计入收敛进度 |
| 未决清单(ledger) | AI 维护的"还没讨论清楚的问题"列表,轮次推进的油表 |
| G1/G2/G3 三道门 | 雏形认可 / 轮次结束 / 授权撰写总设计文档 |
| 发散模式 | 没想法时让 AI 头脑风暴出候选方向 |
| 覆盖维度表 | 七个必答题清单,全打勾才有资格谈授权 |
| 轮次冻结 | 老轮次文档从此只读,批注永远挂在诞生轮 |
| 工作面板 / 事件直播 | 界面右侧;AI 每一步动作实时推送展示 |
| 权限门 | 读项目内文件、写 docs/ 内与 DESIGN.md 自动放行;AUTHORIZATION.md 仅后端可写(AI 视为越权);其余读写(项目外、项目内 docs/ 外)与一切命令执行,需你在界面点同意 |
| 授权痕迹(AUTHORIZATION.md) | 你输入确认词那一刻由系统(非 AI)落下的授权凭证,存在 = 你确已授权撰写总设计文档 |
| 总设计文档(DESIGN.md) | 唯一权威产物 |
| 分模块文档 | 可选后续拆分,不得与总设计文档冲突 |
| 自检 | 用一双干净的眼睛核查总设计文档,分宽松/严格 |
| 自愈 | 中断留下的半成品,重跑同轮覆盖即修复 |
| 只读归档态 | 使命完成后的终态:全部产物可看不可改,重新讨论需另起新目录 |

## 11. 附录:决策追溯

| 决策 | 内容摘要 | 溯源 | 本文位置 |
|------|----------|------|----------|
| D-01 | 架构 A:本地 Web 应用 + Python 薄后端 + 封装 Claude Code | round-1 | §5, §9 |
| D-02 | 划词批注功能必做,排期独立 | round-1 | §4 |
| D-03 | 单机单人、中文为主、纯 Markdown 落盘 | round-1 | §2, §6 |
| D-04 | 命名:discuss-round-N.md / DESIGN.md | round-1 | §6.1 |
| D-05 | 双轨制:大白话即时答,实质批注批量处理 | round-2 | §3.4 |
| D-06 | 后端 FastAPI,前端原生 HTML/JS | round-2 | §9 |
| D-07 | 轮次冻结制 | round-2 | §3.5, §4.4 |
| D-08 | 单一界面走完五阶段 | round-2 | §3.2, §9 |
| D-09 | 收敛判据 = 维度表全绿 | round-2 | §3.6 |
| D-10 | H1:无头 + 事件直播 + 中止 + 权限门 | round-3 | §5 |
| D-11 | 工作面板展示全量事件,可折叠 | round-3 | §4.3 |
| D-12 | 大白话问答落盘、不入清单 | round-3 | §6.2 |
| D-13 | 中断残留重跑覆盖自愈 | round-3 | §5.5, §7.3 |
| D-14 | round-0 基线整体定案(五阶段三道门等) | round-4 | §1, §3 |
| D-15 | 中期接手零专门机制 | round-4 | §2, §5.4 |
| D-16 | 发散模式四条 | round-4 | §3.7 |
| D-17 | 授权门槛与自检机制 | round-4 | §4.4, §8 |
| D-18 | 批注数据结构 | round-4 | §6.2 |
| D-19 | 启动前提与恢复规则 | round-4 | §7 |
| D-20 | AI 生成中途插话确认为非目标(v2 再议) | check-1 后用户裁决 | §1.5, §5.5 |
| D-21 | 使命完成后为只读归档态,重新讨论另起新目录 | check-1 后用户裁决 | §7.4, §8.2, §10 |
