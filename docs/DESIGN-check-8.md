# DESIGN-check-8.md · 严格自检第 8 轮报告(原文完整留存)

> 执行者:「干净的眼睛」——无讨论上下文,仅依据磁盘文件。第 8 轮,不受前七轮锚定。
> 裁决:**FIX**(P2×2,无 P0/P1)
> 趋势对比:r1 13 → r2 9 → r3 6 → r4 7 → r5 3 → r6 3 → r7 3 → r8 2。

---

# DESIGN.md v1.7 · 独立核查报告(第 8 轮)

**总裁决:FIX(P2×2,无 P0/P1)**

趋势:r1 13 → r2 9 → r3 6 → r4 7 → r5 3 → r6 3 → r7 3 → r8 2。

## 一、v1.7 三项修复验证

1. **G1 定稿后端追加标记 + 零完整轮兜底行 2(§4.4/§7.4):** 落实 ✓。round-1 出生即带合规 `> 申请授权:否` → §7.3 完整轮判据对它成立 → 推导链闭合(行 3 可解);后端写「否」同时使 G3 四校验之④必然不满足,恰好封死"从 round-1 直接授权"的解析歧路(缺维度表/未决清单小节时 ②③ 无法解析,但 ④ 已拦截,无错误行为可达)。check-7 问题 1 闭合。
2. **矩阵拆格(§5.4):** 落实 ✓。tmp 由 AI 写、自动放行;直写一律拒绝;与 §7.3①②、§7.4 行 4"半份不可能存在"的绝对保证互证成立;check-7 问题 2 闭合。
3. **术语表同步 .tmp(§10):** 落实 ✓,且把"DESIGN.md 直写/AUTHORIZATION.md 仅后端"一并写入,与矩阵行为等价。

三项均在已有决策方向内(后端写入先例自 v1.3 起被连续四轮确认服务于 D-17/D-19),无越界。但闭合过程暴露矩阵一处既有的行序空洞,见问题 1。

## 二、问题清单

**问题 1|P2|§5.4 权限矩阵泛行 vs .tmp 行、AUTHORIZATION.md 行(前七轮漏检)**
- **描述:** 泛行"写 项目内、docs/ 与 DESIGN.md 之外 → 弹窗"按字面同时覆盖 `DESIGN.md.tmp`(≠DESIGN.md)与 `AUTHORIZATION.md`(项目根、docs/ 外):前者与专项行"自动放行"冲突,后者与专项行"一律拒绝,无任何批准路径"冲突——若实现者按"对象满足哪行条件即用哪行"而不知先取哪条,AUTHORIZATION.md 将获得一条弹窗可批准路径,恰是 check-5 曾封死的越权通道;tmp 则回到 check-6 问题 1 的逐次弹窗。§7.4 推导表因同类问题(check-4 问题 3)补过"自上而下,首条命中即取"声明,§5.4 矩阵至今没有等效声明,泛行条件列也未把两个特殊文件排除。
- **建议修法:** 二选一:矩阵表头加"自上而下,首条命中即取"(与 §7.4 同文);或泛行对象改为"项目内、docs/ 与 DESIGN.md(.tmp)、AUTHORIZATION.md 之外"。

**问题 2|P2|§8.2 修复者停下问用户:裁决的落盘载体与续跑入口未定义**
- **描述:** "修复者必须停下来在界面抛出问题、获用户裁决后才继续"只定义了停与继续,没定义**裁决如何进入下一次修复调用**:修复者被规定"以磁盘为唯一输入",而用户的界面裁决没有任何规定的磁盘落点(报告文件?独立文件?由谁写入?);界面上也不存在承载该回答的输入通道——阶段 5 的交互只有选档、继续按钮与中止,划词批注挂在轮次文档上(DESIGN.md 非轮次文档),且生成中途插话是确认过的非目标(D-20)。工程上只能自行发明"后端代收+落盘+重跑修复"一整套机制,属本文其余部分均已钉死而此处独缺的"谁写、写哪、怎么续"级空白。
- **建议修法:** 补一句职责链,如:修复者抛出的问题由后端截存并暂停;用户在界面回答后,**后端将该裁决追加落盘到当轮核查报告末尾**,并提供"继续修复"按钮重跑修复调用(续跑以含裁决的报告为输入)。

## 三、五维结论

1. **内部一致性:有问题(仅问题 1)。** G1 三种出生方式接缝、tmp/直写/AUTH 四处对齐、推导表互斥+首条命中、四处 G3 校验单一权威、PASS 前缀文法、完成态,逐项对照自洽;残留矛盾集中在权限矩阵泛行与专项行的字面重叠。
2. **历史批注符合度:零问题。** D-01~D-21 逐条对照讨论原文,收录无走样;v1.7 三项新机制均为修复期补全,服务于 D-13/D-17/D-19 既定方向,未改变任何用户可见决策;D-20/D-21 溯源与追溯表记载一致。
3. **决策无歧义:有问题(问题 1、2)。** 两处均迫使实现者自行选择(行序优先级、裁决载体),选错一处会削弱"AI 写 AUTHORIZATION.md 无任何批准路径"的硬规则;其余决策均可独立读懂实施。
4. **可实施性:主体可开工。** 两项均为一句级补写,无架构阻断;问题 1 影响权限门的裁定逻辑,问题 2 属罕见路径(用户职权类问题才触发),建议随下轮修复一并补上。
5. **结论覆盖:零问题。** 追溯表 21 行逐行核对,溯源与"本文位置"全部真实指向(D-13 可指回 §7.3 实现路径);21 条决策正文均有承载;头部宣称的修复计数 13/9/6/7/3/3/3 与七份报告实际数字一致。

> 核查结论:FIX(P2×2)
