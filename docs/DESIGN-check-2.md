# DESIGN-check-2.md · 严格自检第 2 轮报告(原文完整留存)

> 执行者:「干净的眼眼」——无讨论上下文,仅依据磁盘文件。第 2 轮,明确指示不受第 1 轮报告锚定。
> 裁决:**FIX**(P1×3、P2×6,无 P0)
> 中途一次 API 超限中断,恢复后完成;报告按精简纪律产出。

---

# DESIGN.md v1.1 独立核查报告(第 2 轮)

**总裁决:FIX(有 P1/P2——P1×3、P2×6,无 P0)**

## 一、上轮 13 项修复验证

13 项全部落实,方向均忠于报告建议与既有决策,无一项空转:P0-1 统一为单一口径并补「新批注不阻碍申请授权」;P1-2 → 新增 §6.4 文法;P1-3 → §4.4 补 G1/G2 交互;P1-4 → §7.4 推导表 + draft.md 入目录树;P1-5 → §5.4 完整矩阵 + 术语表同步;P1-6 → §3.7 补模板要点(与 round-0 §6 原文核对一致);P2-7/8/9/10/11/13 均逐条到位;P2-12 补录 D-20。追溯表 21 行逐一核对,溯源与位置真实。

修复引入的新问题见下(问题 1、5、6 直接源于修复文本)。

## 二、新发现问题

**1|P1|§8.1 vs §3.6/§4.4(G3 按钮点亮条件)**——同一授权按钮两套条件:§3.6/§4.4 说「工具解析轮次文档三处结构」(未决清单清零+维度表全绿+标记为是);§8.1 资格门槛是四条,多出「上轮批注全部被回应(由 annotations.json 判定)」。按 §4.4 实现会漏掉这道防自报闸,按 §8.1 实现则与另两处不等价。修法:指定一处为权威定义(建议含 annotations 判定共四处),其余交叉引用。

**2|P1|§4.2/§5.1/§6.1(阶段 1-2 会话落盘)**——阶段 1-2 侧栏为「对话流」,但 §5.1 无状态文件驱动要求每次无头调用从磁盘恢复上下文,目录树中只有 draft.md(会话「产出的草稿」,非对话记录):用户发言与 AI 回复落在哪、重启后对话流从哪恢复,全文无答案;直接关系成功判据 3(每轮讨论、每条批注落盘可查)。修法:规定阶段 1-2 会话转录的落盘文件(名字、格式、被后续调用读取的方式)。

**3|P1|§8.2/§7.4(自检循环中的修复者身份)**——宽松档「核查+修复+追加 PASS」、严格档每轮之间的修复由谁执行从未定义:核查执行者若自修则污染「干净的眼睛」的独立性;原流程 AI 修则其调用形态未规定;宽松档 PASS 行由谁「追加」也无主语。修法:明确修复动作的执行方与调用形态(含核查期间产生的 D-20/D-21 式用户裁决环节如何进循环)。

**4|P2|§8.2**——「执行者:只读磁盘资料」与其产出(写 check 报告、修订 DESIGN.md)字面矛盾;本意应为「仅以磁盘信息为输入、不带会话记忆」。修法:改为「只读磁盘资料作为唯一输入」。

**5|P2|§7.4 第 5 行**——该行写死「严格档自检进行中」,但宽松档半成品(报告已落盘、未追加 PASS)重开同样落入此行,只能靠括号里的「档位记录于报告头部」纠偏,行主体与括号自相矛盾。修法:该行改为「自检进行中(档位见报告头部)」。

**6|P2|§6.2/§6.3(回应表格式与 plain 落盘者)**——写回职责依赖「后端解析 AI 产出文档中的批注回应表」回写 answer/status,但回应表的机器格式(列名、批注 id 对应方式)未定义(五件套中唯决策登记被标注为实践归纳,回应表实则同样无规格);且大白话即时答的 answer、落盘动作由谁执行(轻量调用不写文件,§6.2 只规定了批量回写路径)未写。修法:在 §6.3 或 §6.4 补回应表最小格式,并补 plain 类型落盘职责一句。

**7|P2|§6.4(文法与现存样例不符)**——本项目 round-4 实际文本三处均不满足新文法:授权标记为 `> **申请授权:是**`(多粗体星号且不在文末,其后还有 §7)、维度表列为「维度/状态/支撑」(文法要求「说明」)。文档未说明文法生效前历史项目的处理方式;若拿本项目磁盘做工具验收样例,解析全部失败。修法:加一句「文法适用于本设计实施后生成的轮次文档,历史文档不要求回溯改造」。

**8|P2|§3.3(大纲式残留)**——四步之一「逐条回应上轮全部批注(含大白话请求)」与 D-05/D-12 的大白话即时回应直接冲突(大白话已在产生时回答,不存在留待下轮的「大白话请求」)。此为 round-0 原文残留(早于双轨制提出),D-14 整体定案时未清洗。修法:删去「(含大白话请求)」。

**9|P2|§7.4/§5.5(G3 授权后写作中断的恢复)**——授权已给但 AI 写 DESIGN.md 中途崩溃时,磁盘无 DESIGN.md、推导回阶段 3,授权事实无承载;「重跑同一条任务」由谁触发、G3 确认词交互是否重现,未定义。修法:补一条该窗口的恢复规则(如:末轮授权标记为「是」且无 DESIGN.md 时,界面提供「重写总设计文档」入口)。

## 三、五维结论

1. **内部一致性**:有问题(#1 按钮条件分裂、#4、#5、#8)。修复后主口径(三道门、阶段表、冻结、推导、完成态)已自洽,残留矛盾集中在 §8.1 与邻节的细粒度差异。
2. **历史批注符合度**:21 条决策(含 D-20/D-21)逐一对照讨论原文,如实收录无走样;§6.4、§5.4、§7.4、§3.4、§6.2 等修复期新机制均在既有决策方向内、未见与用户确认内容冲突的发明;#2、#3 属讨论从未涉及的空白被带过——无「私货」,但有未声明的新增自由度。
3. **决策无歧义**:多数决策可独立实施;#1(两套点亮条件)与 #3(修复者)会让工程师被迫追问。
4. **可实施性**:主体可开工;缺口为 #2(会话落盘)、#6(回应表格式)、#3(修复者)、#9(恢复路径)。
5. **结论覆盖**:追溯表 21 行逐行核验真实、位置正确;全部已确认决策在正文中均有对应,无遗漏。该维度零问题。

> 核查结论:FIX(P1×3、P2×6)
